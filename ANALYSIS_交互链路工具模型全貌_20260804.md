# 知微 bot 交互链路、工具与模型全貌（2026-08-04 重构后）

> 覆盖从用户飞书消息到 bot 回复的完整链路。每条消息经过的检查点、调用的 LLM 模型、使用的 Prompt 和工具，以及各层之间的数据流关系。

---

## 一、消息入口与总体路由

```
飞书 WebSocket 长连接 (ws_client.py)
  └─ handle_text_async(text, user_id, message_id)
       │
       ├─ [1] 语音确认消费 (pending_voice)
       ├─ [2] 图片追问消费 (pending_image)
       ├─ [3] 视频重复确认消费 (pending_video_confirm)  ← P0.3 新增
       │
       └─ 斜杠命令链 (11 个 handler, 按顺序)
            │
            ├─ dev_commands       (/dev, /status 等)
            ├─ research_commands  (/research, /深度研究 等)
            ├─ knowledge_commands (/ask, /klib, /收录 等)
            ├─ learn_commands     (/learn, /学习 等)
            ├─ system_commands    (/help, /status 等)
            ├─ media_commands     (视频链接识别 → 重复检测 → 管线)
            ├─ podcast_commands   (/podcast 等)
            ├─ youtube_commands   (/yt 等)
            ├─ lark_commands      (/lark 等)
            ├─ agent_commands     (Layer 2/3 智能路由)
            │
            ├─ nl_router          ← 自然语言意图路由 (LLM 分类)
            │    ├─ knowledge_query  → RAG 检索
            │    ├─ learn_concept    → 概念卡片生成
            │    ├─ capture          → 知识库捕获
            │    ├─ research         → 深度研究
            │    ├─ media_followup   → qa / reanalyze  ← P1.2 新增
            │    ├─ status           → 系统状态
            │    └─ chat             → 返回 False, 交回兜底
            │
            └─ ChatHandler 兜底 (commands/chat_handler.py)
                 ├─ Mem0 长期记忆检索
                 ├─ RAG 关键词触发检索
                 └─ llm_client.call_with_session("chat", ...)
```

**关键时序**：一个普通文本消息至少经过 13 个 handler 尝试（11 个斜杠命令 + agent 路由 + nl_router），然后才到 ChatHandler。每个 handler 内部做关键词/前缀匹配，成本极低（sub-ms），但 `agent_commands` 和 `nl_router` 各调用一次 LLM 做意图分类。

---

## 二、每条路径的模型与工具调用

### 路径 A：视频链接 → 笔记 → 追问

```
用户: "0.51 复制打开抖音... https://v.douyin.com/abc123/"

Step 1: media_commands 识别
  is_video_url(text) → True
  extract_video_url(text) → "https://v.douyin.com/abc123/"
  video_history.check_duplicate(url) → None (首次)
  → handle_video_async(text, message_id, user_id)

Step 2: 异步线程 process_video
  subprocess: ~/zhiwei-shared-venv/bin/python3 ~/zhiwei-bot/scripts/douyin_distiller.py
    --from-text "..." --output-dir ~/Documents/ZhiweiVault/.../75_视频笔记_Video-Distill/
    --vision (默认) --cookies ~/zhiwei-bot/secrets/douyin_cookies.txt
    [--instruction-file /tmp/instruction_xxx.txt]  ← P1.3 新增, 有用户指令时注入

  distiller 内部 LLM 调用:
    Stage1: llm_client.call(role="format", model=kimi-k2.5, Coding Plan)
      用途: ASR 转写分类 (内容类型识别 + 实体提取)
      输入: 转写文本前 3000 字
      输出: {"content_type": "tech_analysis", "entities": [...]}

    [只有长视频且 >8000 字时] 分段 key_points:
      llm_client.call(role="format", model=kimi-k2.5)
      用途: 提取每段关键时间戳

    Stage2: llm_client.call(role="research", model=qwen3-max-2026-01-23, Coding Plan)
      用途: 按内容类型深度分析, 输出 JSON (title/core_insight/tier/key_points/
            technical_reconstruction/summary/action_items)
      输入: 转写全文 + 背景知识 + 视觉信息 + [用户指令]
      系统 Prompt: KnowledgeDistiller.SYSTEM_PROMPT (L2925-) 或 STAGE2_PROMPTS[content_type]

    [vision 模式] 抽帧 VLM:
      llm_client.call(role="vision", 走 Coding Plan vision 模型)
      用途: 图表/板书/架构图提取

  RAG 背景增强: zhiwei-rag HTTP (:8765) 混合检索 (bge-large-zh + jieba FTS → RRF → bge-reranker)

Step 3: 产物回写
  process_video 返回 digest → _build_video_digest → reply_message (飞书卡片/文档链接)
  ConversationStore.register_artifact(user_id, "video", url, title, note_path, summary)
  ConversationStore.record_turn(user_id, "system", "视频《title》分析完成", kind="artifact_notice")

  --- 用户追问 ---

用户: "刚才那个视频第三点再展开讲讲"

Step 4: 命令链全落空 → nl_router
  _parse_intent(text):
    llm_client.call(role="format", model=kimi-k2.5, Coding Plan, timeout=15s)
    系统 Prompt: INTENT_PROMPT (nl_router.py L14-39)
    输出: {"intent": "media_followup", "confidence": 0.9, "action": "qa",
           "instruction": "展开讲第三点"}

Step 5: route_natural_language → media_followup 分支
  高置信 (≥0.75) → 直执行
  ConversationStore.get_last_artifact(user_id) → dict
  _exec_media_followup(action="qa"):
    读 note_path 截 4000 字
    llm_client.call_with_session("chat", msg, f"feishu-{user_id}", Coding Plan)
      模型: qwen3-max-2026-01-23
      注入: 8 轮会话历史 (_session_store 内存)
      msg: 【背景：视频《title》的笔记】{note}\n\n【用户追问】{instruction}
    → reply_message + ConversationStore.record_turn 回写问答对
```

### 路径 B：视频链接 → 代称映射 → 重析（8/4 狮驼岭场景）

```
Step 1-3 同上 (视频笔记生成)

用户: "这个博主的狮驼岭是指美国，凤仙郡是指中国，请代入重新分析刚才的视频"

Step 4: nl_router 意图识别
  llm_client.call(role="format", model=kimi-k2.5)
  输出: {"intent": "media_followup", "confidence": 0.92, "action": "reanalyze",
         "instruction": "狮驼岭=美国、凤仙郡=中国、棒子=韩国、鬼子=日本。
         代入这些真实指向还原代称后，重新输出整个视频的观点和摘要"}

Step 5: _exec_media_followup(action="reanalyze")
  ConversationStore.set_instruction(artifact_id, instruction)
  → 写入临时文件 ~/zhiwei-bot/tmp/instruction_{user_id}.txt
  → reprocess_with_instruction(user_id, artifact, instruction, message_id)
  → handle_video_async(text, message_id, user_id, instruction=instruction)
  → process_video(text, message_id, user_id, instruction=instruction)
  → subprocess douyin_distiller.py --instruction-file /tmp/instruction_xxx.txt

  distiller 内:
    instruction 段落拼入 extra_context:
    "**用户指定背景/还原指令（最高优先级）：**\n{instruction}\n
     请在分析与摘要中严格执行上述还原与映射。"
    → 附入 Stage2 background_section (与 vision 视觉信息同通道)

  → 新 digest 生成 → reply_message
  → ConversationStore.register_artifact 覆盖旧条目
```

### 路径 C：纯文本闲聊 / 普通对话

```
用户: "今天有什么AI新闻"

Step 1-2: 命令链全落空
Step 3: nl_router _parse_intent → {"intent": "chat", "confidence": 0.95}
  → route_natural_language 返回 False (chat 意图不消费)

Step 4: ChatHandler 兜底
  Mem0 长期记忆检索:
    mem0_client.build_context(user_id, text, limit=5)
    返回: 与该用户相关的历史记忆片段

  RAG 关键词触发检索 (仅当消息含 "查一下/搜一下/知识库/库里/文档" 等关键词):
    rag_client.get_context(text) → HTTP 调 zhiwei-rag :8765

  构建 enriched_message:
    [Mem0 上下文] + [RAG 上下文] + 当前问题: {text}

  llm_client.call_with_session("chat", enriched_message, f"feishu-{user_id}")
    模型: qwen3-max-2026-01-23 (Coding Plan)
    系统 Prompt: prompts/main_agent.md (知微角色设定)
    注入: 8 轮会话历史 (_session_store 内存)
    超时: 300s

  → reply_message + ConversationStore.record_turn 回写
```

### 路径 D：知识库检索

```
用户: "知识库里关于 HBM4 的笔记有哪些"

nl_router: {"intent": "knowledge_query", "confidence": 0.95}
  → do_knowledge_query(topic, user_id, message_id, ctx)
  → RAG 检索 (zhiwei-rag :8765 HTTP)
  → llm_client.call_with_session("chat", ...) 回答
```

### 路径 E：概念学习

```
用户: "学一下 Muon 这个优化器"

nl_router: {"intent": "learn_concept", "confidence": 0.95}
  → do_learn(concept, user_id, message_id, ctx)
  → llm_client.call(role="research", model=qwen3-max)
     生成概念卡片 (Markdown + frontmatter + [[wikilink]])
  → 写入 ~/Documents/ZhiweiVault/70-79_个人笔记/72_概念索引_MOC/
  → subprocess 调 generate_moc.py 更新 MOC 索引
  → reply_message 返回卡片内容
```

---

## 三、模型使用全景

```
┌──────────────┬───────────────────────────────┬───────────────────────────────┬──────────┐
│   角色 (role) │        Coding Plan 模型        │            用途              │ 调用频率 │
├──────────────┼───────────────────────────────┼───────────────────────────────┼──────────┤
│ format       │ kimi-k2.5                     │ 意图识别、ASR 分类、chunk     │ 极高     │
│              │ (实测: 意图 1.6s, 分类 3.6s)   │ key_points 提取               │ (每条消息)│
├──────────────┼───────────────────────────────┼───────────────────────────────┼──────────┤
│ chat         │ qwen3-max-2026-01-23          │ 兜底对话、追问回答、知识问答  │ 高       │
│              │ (实测: 32s, 5 语料 8/8)        │                               │          │
├──────────────┼───────────────────────────────┼───────────────────────────────┼──────────┤
│ research     │ qwen3-max-2026-01-23          │ 视频蒸馏 Stage2、概念学习、   │ 中       │
│              │                               │ 深度研究                       │          │
├──────────────┼───────────────────────────────┼───────────────────────────────┼──────────┤
│ vision       │ (Coding Plan vision 模型)     │ 视频抽帧图表/板书 VLM 分析    │ 低       │
│              │                               │ (默认开, 无图自动跳过)         │          │
└──────────────┴───────────────────────────────┴───────────────────────────────┴──────────┘
```

**降级链** (auto 模式, 所有角色默认走此路径):
```
百炼 Coding Plan (qwen3-max/kimi-k2.5)
  └─ fallback: glm-5 → kimi-k2.5
       └─ DeepSeek (deepseek-v4-pro, 按量极便宜, 三防)
            └─ 智谱 glm-4-flash (默认关)
                 └─ DashScope (默认关)
                      └─ OpenRouter free (默认关)

（火山 coding plan 原为三防, 已于 2026-08-12 退役移除）
```

**Token Plan** (Anthropic 兼容, Bearer, 白天一折 22:00-08:00 折上二折): 仅当调用方显式传 `prefer_api="token_plan"` 时使用, 已从 auto 降级链移除。当前仅研究意图的深度分析入口触发。

**LongCat** (LongCat-2.0, 按量付费, 条款允许自动化): 仅当显式 `prefer_api="longcat"` 时使用。

---

## 四、工具与外部依赖

| 工具 | 地址/路径 | 触发场景 | 调用方式 |
|---|---|---|---|
| zhiwei-rag 混合检索 | HTTP :8765 | knowledge_query、ChatHandler 关键词、distiller 背景增强 | `core/rag_client.py` HTTP |
| Mem0 长期记忆 | 外部服务 | 每条 ChatHandler 兜底消息 | `core/mem0_client.py` |
| 飞书文档同步 | feishu_note_sync.py | 视频笔记生成后 | 同步 Obsidian 笔记为飞书文档 |
| 飞书卡片 | feishu_api.py | capture 确认、research 确认 | 交互式卡片按钮 |
| douyin_distiller | subprocess | 视频链接处理 | `~/zhiwei-shared-venv/bin/python3` |
| ConversationStore | conversation.db | 会话上下文持久化 | SQLite WAL |
| video_history | video_history.db | 重复视频检测 | SQLite WAL |
| message_log | message_log.db | 入站审计 | SQLite |
| generate_moc.py | subprocess | learn_concept 后 | 更新概念 MOC 索引 |
| idea_capture.py | subprocess | 灵机一动想法捕捉 | arxiv-paper-analyzer 脚本 |

---

## 五、LLM 调用次数分析（每条消息）

| 消息类型 | LLM 调用次数 | 明细 |
|---|---|---|
| 视频链接 | 2-4 次 (异步) | distiller stage1 (format) + stage2 (research) + [分段 key_points (format)] + [vision VLM] |
| 视频追问 (qa) | 2 次 | nl_router 意图识别 (format) + 回答 (chat) |
| 视频追问 (reanalyze) | 3-5 次 (异步) | 意图识别 (format) + distiller 全流程 |
| 普通文本 (chat) | 1 次 | nl_router 意图识别 (format) — 如果判为 chat, 再加 1 次 chat 回答 |
| 知识库检索 | 2 次 | 意图识别 (format) + chat 回答 |
| 概念学习 | 2-3 次 | 意图识别 (format) + research 生成卡片 + [MOC 更新] |
| 「继续」/「确认」等短词 | 0-1 次 | 如果被 pending 消费则 0 次; 否则走 nl_router 意图识别 |

**关键事实**: 每条非命令消息都至少触发 1 次 LLM 意图识别 (format role, kimi-k2.5)。意图识别模型最快 (1.6s), 但高频调用累积成本可观。

---

## 六、上下文层全景（重构后）

```
┌──────────────────────────────────────────────────────────────────┐
│                      飞书消息入站                                  │
└──────────┬───────────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Layer 0: 关键词/前缀消费                                         │
│  pending_voice / pending_image / pending_video_confirm            │
│  斜杠命令链 (11 handlers)                                        │
│  成本: 0 LLM 调用, sub-ms                                        │
└──────────┬───────────────────────────────────────────────────────┘
           │ 未消费
           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Layer 1: agent_commands (zhiwei_agent)                          │
│  如果 AgentExecutor 有 get_intent → LLM 意图分类                  │
│  Layer 2 → 工作流执行; Layer 3 → 转发 (已退役)                    │
│  成本: 可能 1 次 LLM 调用                                        │
└──────────┬───────────────────────────────────────────────────────┘
           │ 未消费
           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Layer 2: nl_router (自然语言意图路由)                            │
│  _parse_intent → LLM (format/kimi-k2.5, 15s)                     │
│  7 种意图: knowledge_query / learn_concept / capture / research   │
│            media_followup / status / chat                        │
│  成本: 1 次 LLM 调用 (每条消息)                                   │
└──────────┬───────────────────────────────────────────────────────┘
           │ 未消费 (chat 意图)
           ▼
┌──────────────────────────────────────────────────────────────────┐
│  Layer 3: ChatHandler 兜底                                        │
│  Mem0 长期记忆 → RAG 关键词检索 → call_with_session               │
│  成本: 1 次 LLM 调用 (chat/qwen3-max) + Mem0 网络调用             │
└──────────┬───────────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────────┐
│  上下文持久化: ConversationStore                                  │
│  - turns 表: 最近 50 轮对话 (user/assistant/system)              │
│  - artifacts 表: 最近媒体产物 (video/article/podcast)            │
│  - build_context(): 注入 ChatHandler 兜底前                       │
│  - 并行: llm._session_store (内存, 8 轮, 重启即失)               │
│  - 并行: ws_client.chat_history (截断 100 字, 仅展示)            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 七、可优化点（诊断清单）

### 1. 意图识别与 agent_commands 可能重复调用 LLM
`agent_commands` 的 `_agent_executor.get_intent()` 和 `nl_router._parse_intent()` **各自独立调用一次 LLM 做意图分类**。如果 `agent_commands` 有 `get_intent` 方法, 每条消息会触发 2 次意图分类 LLM 调用。建议在 agent_commands 返回 False 后, nl_router 复用同一个意图结果, 或合并为一层。

### 2. 简单关键词消息仍走 LLM 意图分类
"继续"、"确认"、"取消"、"好的" 等短词在 pending_video_confirm 消费后已避免, 但其他短词（如"讲讲"、"展开"、"还有吗"）仍会经过 nl_router 的 LLM 意图分类。可增加关键词预筛: 消息长度 < 10 字 + 不含链接 + 有最近产物 → 直接走 media_followup-qa, 跳过 LLM 意图分类。

### 3. ChatHandler 兜底的全量模型偏重
所有 chat 调用都用 qwen3-max-2026-01-23（32s, 最强但最慢）。对于简单追问、确认回复等, 可降级到 kimi-k2.5 或 glm-5。建议: 根据消息长度和上下文复杂度动态选模型。

### 4. Mem0 每条消息都调用
ChatHandler 兜底每条消息都检索 Mem0 外部服务。对于高频短消息, 可加缓存或限流。

### 5. _session_store 与 ConversationStore 双写
`llm.call_with_session` 维护自己的内存级 `_session_store`（8 轮）, ConversationStore 维护持久化 `turns` 表。两者内容重叠但独立, 重启后 `_session_store` 丢失。可统一为 ConversationStore 单一来源, 减少内存占用。

### 6. 视频蒸馏 Stage1 分类可缓存
同一视频重复处理时, Stage1 的 ASR 分类结果可复用（转写文本不变, 内容类型不变）。当前每次重跑都重新分类。

### 7. distiller 作为子进程开销大
每次视频分析都启动新 Python 子进程（冷启动 import 开销 ~5-10s）。可考虑改为常驻 worker 或直接 import 模块调用（但需注意 venv 隔离）。

### 8. 意图识别 Prompt 随功能增长膨胀
INTENT_PROMPT 现已 7 种意图 + 示例 + 输出格式, 约 40 行。每次新增意图都在加长 Prompt, 增加 token 消耗和延迟。可考虑分层: 先粗分类(内容/命令/追问) → 再细分类, 减少单次 Prompt 长度。

---

## 八、当前重构后的关键改进

| 改进点 | 重构前 | 重构后 |
|---|---|---|
| 视频追问 | 无上下文, 泛泛而谈 | media_followup-qa: 读 note_path + 回答 |
| 代称映射重析 | 不支持 | media_followup-reanalyze: set_instruction + distiller --instruction-file |
| 重复链接「继续」 | 假承诺, 无消费 | pending_video_confirm 登记 + 消费 |
| 兜底对话 | 每轮先 docker exec 失败再降级 | 直连 LLM, 延迟下降 |
| 聊天上下文 | 内存级 _session_store, 重启即失 | ConversationStore 持久化, 重启可读 |
| 媒体产物 | 从不回写会话 | register_artifact + record_turn, 上下文可见 |